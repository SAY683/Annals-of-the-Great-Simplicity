# 04 · 维护与故障排查

> 维护原则：**任何修改前先备份**（Move-Item 旧目录，勿删）；改配置后按需重建索引。

## 1. 症状速查表

| 症状 | 原因 | 处理 |
|---|---|---|
| 查询报"Project ... has no output/ directory" | GRAPHRAG_PROJECT 被设为错误路径 / 默认路径未命中 | 显式传 `project=` 参数；或检查环境变量 |
| MCP 返回路径乱码（如 `鏀荤暐`） | 宿主进程环境里的中文路径被 GBK 误解码 | 本归档路径全 ASCII，无此问题；工作副本已加多候选路径容错 |
| 查询超时/无响应 | CC Switch 未运行 / embedding 服务未启动 | 启动 `00-start-embedding.bat`；确认 CC Switch 在 15721 端口 |
| 输出为空但 usage 全是 reasoning_tokens | 丢了 `reasoning_effort: none` | 检查 settings.yaml `call_args.extra_body.reasoning_effort: none` |
| 重建时大量"Community Report Extraction Error" | 模型输出 JSON 带围栏/尾逗号/未转义引号 | 确认 `community_reports_extractor.py` 三层容错补丁在位（见下） |
| opencode 429 限流 | Go 计划 5 小时/周/月配额 | 降 `concurrent_requests` 到 12；或换时段 |

## 2. 为什么"这么快"（关键背景）

opencode 的 deepseek-v4-flash **默认思考全开**：会把全部 max_tokens 烧在 thinking 上，
输出为空，单块 3-4 分钟（这是旧结论"opencode 慢"的真相）。关闭思考后单块 ~40s，
24 并发全量索引 ~35 分钟，费用 0。**配置里必须保留 `reasoning_effort: none`。**

## 3. 依赖服务自检

```powershell
# 向量服务（8081）
Test-NetConnection 127.0.0.1 -Port 8081
Invoke-RestMethod http://127.0.0.1:8081/v1/models

# LLM 网关（CC Switch，15721）
Invoke-RestMethod http://127.0.0.1:15721/v1/models
```

## 4. 重建索引（三种情况）

1. **只改配置/prompt**（实体类型、prompt 文本）：`scripts\03-rebuild-index.bat`
   —— LLM 缓存可复用部分结果，速度快。
2. **换了输入文本**：替换 `project\input\` 后重建。
3. **全新机器**：先 `pip install graphrag`，启动依赖，再重建。

> 重建前务必：embedding 服务在线（8081）+ LLM 通道在线（CC Switch 15721）。
> 设置 `PYTHONIOENCODING=utf-8` 与 `PYTHONUTF8=1` 避免中文编码报错（脚本已内置）。

## 5. 社区报告 JSON 三层容错补丁（重要）

位置：`G:\Python\Lib\site-packages\graphrag\index\operations\summarize_communities\community_reports_extractor.py`
（升级 graphrag 包会被覆盖，需重打；归档内 `mcp\` 与本文档记录了方法）

三层防御（`_extract_json` + json_repair 兜底）：
1. 剥离 markdown 代码围栏（```json ... ```）
2. 截取首 `{` 到末 `}` + 清除尾逗号
3. `json_repair.loads` 兜底（处理字符串内未转义 ASCII 引号，如 `The "O|O"...`）

实测效果：947 失败 → 4 → 1 → 0（500/500）。

## 6. 顽固社区报告的处理方法

个别社区（如 379）可能因上下文特殊而持续失败。处理：
1. 用 GraphRAG 自己的上下文构建器复现生成（`build_local_context` + `build_level_context`）。
2. 将结果补插 `community_reports.parquet`（需按 schema 生成 `id`=sha512(full_content)、
   `human_readable_id`=community、parent/children/size/period 取自 communities 表）。
3. 同步向 `lancedb/community_full_content.lance` 补一条 1024 维向量（用本地 bge-m3 生成）。
完整代码模式见 `图谱项目训练经验归档.md`。

## 7. 常见配置陷阱

- 改了 embedding 模型忘了改 `vector_size` → LanceDB 维度不一致报错。
- `api_key` 对 CC Switch 无所谓（填 `dummy`）；直连 DeepSeek 时必须填真实 key。
- 中文路径在部分工具链（PowerShell→python 管道）会乱码 → 用环境变量传路径，或保持归档路径全 ASCII。

## 8. Windows 中文编码大坑（本归档曾踩过）

**现象**：部分文档出现大段 `?`（中文全部丢失）。审计脚本见下方。

**根因**：`PowerShell 管道 -> python stdin`（如 `@'...'@ | python -`）时，PowerShell 用控制台代码页（GBK）把中文写入管道，python 读到的中文变成 `?`。凡是用这种管道写的文件都会损坏。

**安全写法**（本归档已全部按此修正）：
- 写文件用 **PowerShell `Set-Content -Encoding UTF8`**（here-string 内存中是 Unicode，可靠）
- 给 python 传中文参数/路径用**环境变量**（`$env:XXX = '中文'`，python 读 `os.environ`）
- 不在 `@'...'@ | python -` 的 heredoc 里放中文

**快速审计**（检测整目录是否有中文损坏）：
```python
import os, re
for root, _, files in os.walk(r"."):
    for fn in files:
        if fn.endswith((".md", ".ps1", ".py", ".txt")):
            t = open(os.path.join(root, fn), encoding="utf-8", errors="replace").read()
            if t.count("\ufffd") or re.search(r"\?\?{2,}", t) and not re.search(r"[\u4e00-\u9fff]", t):
                print("CHECK:", os.path.join(root, fn))
```
（2026-08-05 实测：修复 3 个文件后全目录 0 损坏。）

## 9. 分块器边界 U+FFFD（tiktoken 解码伪影）

**现象**：`text_units.parquet` 中约 0.013% 字符（14 个）被替换为 U+FFFD，多出现在**块首**。

**根因**：`split_single_text_on_tokens`（graphrag\index\text_splitting\text_splitting.py）用 tiktoken 按 token 切块后 `decode()` 还原文本——当 overlap 切点落在多字节 UTF-8 序列中间时，tiktoken 解码产生 U+FFFD。源文本（documents.parquet）始终干净，这是**分块器解码伪影**，不是数据损坏。

**影响**：basic / drift 搜索的文本块轻微失真；向量库中对应块的向量略有偏差。

**修复（2026-08-05 已执行，源 + 归档同步）**：
1. 用 `documents.parquet`（干净全文）按上下文还原每个 U+FFFD 为正确字符；
2. 校验修复后的块是文档的干净子串（全部通过）；
3. 对受影响的 13 个块重新生成 bge-m3 向量，更新 `text_unit_text.lance`（delete+add，行数保持 119）。

**预防**：若重建索引后再次出现，可对 `split_single_text_on_tokens` 的 `decode` 结果做后处理（对块首/块尾的 U+FFFD 用相邻块上下文修正）；或用非 token 型 chunker（如按分隔符）。

## 10. 检索侧 response_format 补丁（opencode/DeepSeek 不兼容，四方法全通）

**现象**：`--method drift` 报 `HTTP 400: This response_format type is unavailable now`（CC Switch/DeepSeek 不支持部分 response_format 类型）。

**根因**：drift 搜索硬编码发送 `response_format`（primer 用 json_schema、local 子步骤用 json_object），opencode 后端 400 拒绝。

**补丁**（G:\Python\Lib\site-packages\graphrag\，升级 graphrag 会被覆盖需重打）：
1. `query\structured_search\drift_search\primer.py`：去掉 `response_format=PrimerResponse`，改为 `try_parse_json_object(content)` + `PrimerResponse.model_validate(...)`
2. `query\structured_search\drift_search\search.py`：从 local 子步骤 model_params 去掉 `"response_format_json_object": True`
3. `query\context_builder\rate_relevancy.py`：去掉 `response_format_json_object=True`（防御性，dynamic_community_selection 启用时生效；该函数本就带 try_parse_json_object 兜底）

**效果**：global / local / basic / drift **四种检索全部实测通过**（2026-08-05）。

**原则**：凡向 opencode/DeepSeek 发请求，一律不传 `response_format` / `response_format_json_object`，靠 prompt 约束 + `try_parse_json_object`（内部含 json_repair）解析。

## 11. 升级后一键重打补丁（推荐流程）

`pip install --upgrade graphrag` 后，site-packages 里 4 处补丁会被覆盖。**不用手动改**：
```bat
scripts\04-reapply-patches.bat
```
或命令行：
```bat
python scripts\reapply-graphrag-patches.py
```
脚本幂等：已打→跳过；未打→自动打；版本变化找不到锚点→明确告警。已验证"从零重打 + 幂等复跑"双场景。
