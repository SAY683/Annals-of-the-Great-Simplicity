# EDM-Takens Skill — Runtime Dependencies (lower bounds)
# Use this for broad portability. For exact reproducible versions, see
# requirements-lock.txt (the pinned manifest from the reference environment).
#
# Install: pip install -r requirements.txt
#
# 分层说明:
#   [core]  — EDM 核心算法，必须安装
#   [opt]   — 可选优化（Round 20 新增，提升算法辨别性）
#   [test]  — 测试框架

# ── [core] EDM 核心算法 ──
numpy>=1.22
scipy>=1.8
pandas>=1.4
matplotlib>=3.5
pyEDM>=1.0          # optional but recommended for C++ fidelity; numpy fallback exists
scikit-learn>=1.0   # optional, used by some pyEDM paths
statsmodels>=0.14   # optional — Secret 8 (Stationarity Gate: ADF/KPSS tests)

# ── [opt] 算法优化 (Round 20 新增) ──
ruptures>=1.1       # Bai-Perron 结构断点检测 (替代 50% 丢弃方案)
arch>=5.0           # GARCH 波动率建模 (HAVOK 残差分析)

# ── [test] 测试 ──
pytest>=7.0

# Example-specific extras:
#   examples/yinshen/ requires pypinyin (see examples/yinshen/requirements.txt)
