# trace-to-edm — Runtime Dependencies
# =====================================
# 三层桥接编排器（L1 元SCM / L2 世俗语义 PCA / L3 八正道神圣投影）
# 安装: pip install -r requirements.txt
#
# 分层说明:
#   [core]  — 三层投影与 CSV 构建，必须安装
#   [opt]   — 可选优化

# ── [core] 三层投影数学基础 ──
numpy>=1.22
scipy>=1.8                  # SVD (Procrustes 对齐) + 统计检验
scikit-learn>=1.0           # PCA (L2 世俗语义层)

# ── [opt] 算法优化 (Round 20 新增) ──
# 注: ruptures 用于 L1 元SCM 的结构断点检测，当前为可选增强
# ruptures>=1.1
